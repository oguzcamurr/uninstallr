name: CI (macOS proof)

on:
  push:
    branches: ['**']
  pull_request:
    branches: ['**']
  workflow_dispatch:

jobs:
  macos-ci:
    runs-on: macos-latest
    steps:
      - name: UNIQUE_ASSERT_TOKEN_12345
        run: echo "If you don't see this line, you're running a different workflow."

      - uses: actions/checkout@v4

      - name: Assert macOS
        run: |
          echo "RUNNER_OS=$RUNNER_OS"
          echo "HOME=$HOME"
          echo "Workflow ref: ${{ github.workflow_ref }}"
          echo "SHA: ${{ github.sha }}"
          uname -a
          if [ "${RUNNER_OS}" != "macOS" ]; then
            echo "Not on macOS. Wrong workflow/runner triggered."
            exit 1
          fi
          sw_vers || true
          echo "PWD=$(pwd)"
          ls -la
          echo "Top of uninstallr.sh:"
          head -n 20 uninstallr.sh || true

      - name: Ensure script is executable
        run: chmod +x uninstallr.sh

      - name: Install ShellCheck
        run: brew update && brew install shellcheck

      - name: Lint with ShellCheck
        run: shellcheck -x uninstallr.sh

      - name: Dry-run test (tolerate non-zero)
        continue-on-error: true
        run: ./uninstallr.sh --dry-run "Spotify"

